[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "chemunited-sim"
version = "0.1.0"
description = "Dynamic simulation engine for fluidic automation platforms built on chemunited-core."
license = { file = "LICENSE" }
authors = [
    { name = "Samuel Vitor Saraiva" },
]
requires-python = ">=3.11"
dependencies = [
    "chemunited-core @ git+https://github.com/automatedchemistry/chemunited-core.git",
    "chemunited-workflow @ git+https://github.com/automatedchemistry/chemunited-workflow.git",
    "scipy>=1.12",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.29",
    "pyvis>=0.3.2",
]

[project.scripts]
chemunited-sim = "chemunited_sim.cli.main:main"

[project.optional-dependencies]
dev = [
    "black>=25.1.0",
    "mypy>=1.19.1",
    "pre-commit>=4.0",
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.15.7",
]
tray = [
    "pystray>=0.19",
    "pillow>=10",
]

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/chemunited_sim"]

[tool.black]
line-length = 88
target-version = ["py311"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
src = ["src", "tests"]
target-version = "py311"

[tool.ruff.lint]
fixable = ["I"]
select = ["E", "F", "I"]

[tool.ruff.lint.isort]
known-first-party = ["chemunited_sim"]

[tool.mypy]
explicit_package_bases = true
files = ["src/chemunited_sim"]
ignore_missing_imports = true
mypy_path = "src"
pretty = true
python_version = "3.11"
show_error_codes = true

[tool.coverage.run]
source = ["src/chemunited_sim"]

[tool.bandit]
# B101 (assert_used): style nit, not a vulnerability check — we use asserts as
# internal invariants after a preceding guard already rules out the failing case.
skips = ["B101"]
